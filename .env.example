# ──────────────────────────────────────────────────────────────────────
# Personaut PDK — Environment Configuration Template
# ──────────────────────────────────────────────────────────────────────
# Copy this file to `.env` and fill in the values for your setup:
#
#   cp .env.example .env
#
# NEVER commit your `.env` file — it is already listed in .gitignore.
# ──────────────────────────────────────────────────────────────────────

# ── LLM Provider (pick ONE) ──────────────────────────────────────────

# Google Gemini (recommended)
# GOOGLE_API_KEY=your_google_api_key_here

# OpenAI
# OPENAI_API_KEY=your_openai_api_key_here

# AWS Bedrock
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_DEFAULT_REGION=us-east-1

# Ollama (local, no key needed — just run `ollama serve`)
# PERSONAUT_LLM_PROVIDER=ollama

# ── Model Override (optional) ────────────────────────────────────────
# Override the default model for the selected provider.
# PERSONAUT_LLM_MODEL=gemini-2.0-flash

# ── Flask Secret Key ─────────────────────────────────────────────────
# Used for session signing. If omitted, a random key is generated at
# startup (sessions won't survive restarts).
# Generate a strong key with: python3 -c "import secrets; print(secrets.token_hex(32))"
# PERSONAUT_SECRET_KEY=your_secret_key_here

# ── Storage / Persistence ────────────────────────────────────────────
# Type: "sqlite" (default, persists to disk) or "memory" (in-memory only)
PERSONAUT_STORAGE_TYPE=sqlite

# Path to the SQLite database file (only used when STORAGE_TYPE=sqlite).
# Can be absolute or relative to the working directory.
# Generated portraits and videos are stored alongside the DB:
#   ./data/personaut.db      ← database
#   ./data/portraits/         ← generated portrait images
#   ./data/videos/            ← generated conversation videos
#   ./data/simulations/       ← saved simulation results
PERSONAUT_STORAGE_PATH=./data/personaut.db
